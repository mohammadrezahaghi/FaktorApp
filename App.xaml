<Application x:Class="FactorApp.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:tb="http://www.hardcodet.net/taskbar" 
             >
    
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="DeepPurple" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <SolidColorBrush x:Key="SidebarBackgroundBrush" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="SidebarForegroundBrush" Color="#FFFFFF"/>
            <FontFamily x:Key="PersianFont">./Fonts/#IRANSansWeb</FontFamily>

            <Style TargetType="{x:Type Window}" BasedOn="{StaticResource MaterialDesignWindow}">
                <Setter Property="TextElement.Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
            </Style>

            <Style TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignTextBlock}">
                <Setter Property="FontFamily" Value="{StaticResource PersianFont}"/>
            </Style>

            <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="FontFamily" Value="{StaticResource PersianFont}"/>
            </Style>

            <tb:TaskbarIcon x:Key="MyNotifyIcon"
                            IconSource="pack://application:,,,/logo.ico"
                            ToolTipText="شایان کوچولو">
                
       <tb:TaskbarIcon.ContextMenu>
    <ContextMenu FlowDirection="RightToLeft" 
                 FontFamily="{StaticResource PersianFont}" FontSize="14"
                 Style="{StaticResource MaterialDesignContextMenu}">
        
        <MenuItem StaysOpenOnClick="True" IsHitTestVisible="False">
            <MenuItem.Header>
                <StackPanel Orientation="Horizontal">
                    <Image Source="pack://application:,,,/logo.ico" Width="24" Height="24" Margin="0,0,10,0"/>
                    <TextBlock Text="شایان پلاس" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
            </MenuItem.Header>
        </MenuItem>
        
        <Separator/>

        <MenuItem Header="باز کردن برنامه" Command="{Binding OpenCommand}">
            <MenuItem.Icon>
                <materialDesign:PackIcon Kind="ApplicationExport"/>
            </MenuItem.Icon>
        </MenuItem>

        <MenuItem Header="تغییر پوسته (شب/روز)" Command="{Binding ToggleThemeCommand}">
            <MenuItem.Icon>
                <materialDesign:PackIcon Kind="ThemeLightDark"/>
            </MenuItem.Icon>
        </MenuItem>
        
        <Separator/>
        
        <MenuItem Header="خروج کامل" Command="{Binding ExitCommand}">
            <MenuItem.Icon>
                <materialDesign:PackIcon Kind="ExitToApp" Foreground="#FF5252"/>
            </MenuItem.Icon>
        </MenuItem>
        
    </ContextMenu>
</tb:TaskbarIcon.ContextMenu>
            </tb:TaskbarIcon>
        </ResourceDictionary>
    </Application.Resources>
</Application>