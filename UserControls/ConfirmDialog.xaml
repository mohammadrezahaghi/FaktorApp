<UserControl x:Class="FactorApp.UI.UserControls.ConfirmDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             Width="350" Height="Auto">

    <UserControl.Resources>
        <sys:Boolean x:Key="TrueValue">True</sys:Boolean>
        <sys:Boolean x:Key="FalseValue">False</sys:Boolean>
    </UserControl.Resources>

    <materialDesign:Card UniformCornerRadius="10" Padding="20" Background="{DynamicResource MaterialDesignPaper}">
        <StackPanel>
            <materialDesign:PackIcon Kind="QuestionMarkCircle" 
                                     Width="50" Height="50" 
                                     HorizontalAlignment="Center" 
                                     Foreground="{DynamicResource SidebarForegroundBrush}"
                                     Margin="0,0,0,15"/>

            <TextBlock Text="تایید عملیات" 
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}" 
                       HorizontalAlignment="Center" 
                       FontFamily="IranSans"
                       Margin="0,0,0,10"/>

            <TextBlock x:Name="TxtMessage" 
                       Text="آیا از انجام این کار مطمئن هستید؟" 
                       TextWrapping="Wrap" 
                       TextAlignment="Center"
                       Opacity="0.8"
                       Margin="0,0,0,25"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                
                <Button Content="بله، مطمئنم" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="{StaticResource TrueValue}"
                        Margin="5" Width="110"/>

                <Button Content="انصراف" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="{StaticResource FalseValue}"
                        Margin="5" Width="110"/>
            </StackPanel>
        </StackPanel>
    </materialDesign:Card>
</UserControl>