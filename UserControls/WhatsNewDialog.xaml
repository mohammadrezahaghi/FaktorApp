<UserControl x:Class="FactorApp.UI.WhatsNewDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Width="450" FontFamily="Segoe UI" FlowDirection="RightToLeft"
             Background="Transparent">

    <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp8" 
                         Background="Transparent" 
                         BorderThickness="0">

        <Border Background="{DynamicResource MaterialDesignPaper}" 
                CornerRadius="15" 
                ClipToBounds="True">
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <materialDesign:ColorZone Mode="PrimaryMid" Grid.Row="0" Padding="20">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Background="White" CornerRadius="50" Width="60" Height="60" Margin="0,0,0,10">
                            <materialDesign:PackIcon Kind="RocketLaunch" Width="30" Height="30" 
                                                     Foreground="{DynamicResource PrimaryHueMidBrush}" 
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        
                        <TextBlock x:Name="TxtTitle" Text="عنوان آپدیت" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtVersion" Text="نسخه..." Opacity="0.8" FontSize="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </materialDesign:ColorZone>

                <StackPanel Grid.Row="1" Margin="25">
                    <TextBlock x:Name="TxtDescription" Text="توضیحات..." Margin="0,0,0,15" Opacity="0.8"/>

                    <ItemsControl x:Name="LstFeatures">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <materialDesign:PackIcon Kind="CheckCircle" Foreground="Green" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding}" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <Button Grid.Row="2" Margin="20" Content="متوجه شدم" 
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"/>
            </Grid>
        </Border>
    </materialDesign:Card>
</UserControl>